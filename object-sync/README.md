# object-sync — absolute world position + rotation for a droppable prop (Module)

A prop that can be set down anywhere in the world and is in the same place for everyone, including a player who joins after it was dropped: a two-stage contact measure quantizes its world transform on the wearer's client and `word-channel` replicates the bits, with no physbone, no Rigidbody, and no synced position of its own. The packaged novelty is the redundant fine stage — a second, bias-cancelling readout wide enough to absorb the coarse stage's error — which puts millimetre placement anywhere in a ±8192 m world inside **29 synced bits** — a 28-bit transport wire plus the one enable bit — refreshed end to end in ~0.7 s.

Three built configurations ship, each with its own regenerated document, compiled controller and prefab, so a download is usable without this workspace: the entry root (one object, full rotation, 72 rotation bits on the wire), `y/` (one object, heading only, 24 rotation bits, the same 29 synced bits total) and `y_double/` (two objects, heading only, time-multiplexed onto one measure rig, 30 synced bits: a 29-bit wire plus the shared enable). One build per avatar, whichever variant — all three share the parameter prefix and the collision tags, so the single-instance rule under **Seam** spans them.

## Provenance

VRLabs **Custom-Object-Sync** (MIT, © VRLabs) is the studied ancestor. Two of its idioms are re-derived here: the successive-approximation threshold walk that turns an analog reading into bits, and the setter tree that drives a constraint's `PositionOffset` from decoded values. Its receive side is rejected wholesale — a multi-frame decode walk is exactly what a culling pause interrupts, so every decode here is a blend tree holding no state between frames — and so is its physics world-pin and its six rotation physbones. The transport is `vrc-patterns/word-channel`, whose protocol descends from VRCFury's Parameter Compressor. Nothing is ported from either; both are credited as ancestors.

## Interface

**The consumer surface is three roles, and each is one node**: `Anchor` is yours to drive, `Container` under it is the entry's to drive, `Authority` is what gets measured — COS's `SyncPosition Target` / `SyncPosition` / `Target` trio under our names. Your prop goes under `Anchor/Container`, and for a carried prop you add your constraint sources to `Anchor`'s shipped (empty) constraint. Nothing else is an install step.

The split is a two-writer rule, and it is what every composed behavior hangs off: the entry never binds `Anchor` (`--check` holds that), and you never touch `Container`'s constraint. Carry, stow, world-drop, or a RemyDoll-grade authority multiplexer are all constraints and clips you compose against `Anchor`; the entry just measures `Authority` and reconstructs, continuously, with no modes. Locally the entry never touches the prop — `Container` rides `Anchor` by plain hierarchy, zero decode latency, nothing to fight a grab — and `ObjectSync/Enable` off is hands off on every client. While the prop is carried with Enable on, remotes track the reconstruction, ~1.3 s behind the true hand; drop is where this entry converges exactly, carry is where a composed remote-side swap could someday do better. No per-object sync bit, because there is no runtime object index — multi-object is a list in `generate.py`'s CONFIG, resolved at generation time into that many objects' worth of words.

- **Drop, as shipped**: the `Drop` toggle (a VRCFury `Toggle` on `Anchor`, its own synced bool — one bit on top of the entry's 29, deleted with the component if you never drop) animates `Anchor`'s constraint `FreezeToWorld`. On a sourced constraint every 0→1 edge re-captures — pin where it stands, release back to the source (`../../docs/runtime.md` §Constraints owns the measurement) — so with a hand source wired, Drop-on freezes the prop in world for the wearer instantly and the wire walks every other client to the same millimetre. **Wire a source first**: source-less, `FreezeToWorld` captures once, permanently, and nothing animatable resets it. Keep Enable on while dropping — the freeze is per-client, so only the reconstruction, not the frozen anchor, is the same place for everyone.

- **Params in**: `ObjectSync/Enable` (bool, synced, **unsaved**, default off) — the master gate, on `globalParams`, so an outside driver or OSC can reach it under that bare name. Nothing else: the module measures a transform, not a parameter — the prefab ships an `Authority` node at its root as the source of `Rig/Prop/Coarse/Sender`, `Rig/Prop/Fine/Sender` and `Rig/Prop/Rot/Holder`, constrained by default to `Anchor`, so the shipped rig measures the frame the consumer actually drives. Re-point `Authority` only if some other transform is the prop's authority (a grab bone deeper in your prop, say); never at `Container` or anything under it — measured input stays upstream of driven output, or the rig measures its own reconstruction. All three senders must name the same transform or the position and rotation stages measure different objects.
- **Menu**: two bare VRCFury `Toggle`s, no menu asset (`CONVENTIONS.md` §The Interface stanza) — `Object Sync` on `ObjectSync/Enable` from the controller's menu block, and `Drop` from the composed toggle on `Anchor`, which is deletable composition rather than module surface. The module is single-instance per avatar regardless, for the collision-tag reason under **Seam**.
- **Params out** (every client): `Prop/PX/{C,F}` + `Prop/PX/{C0..C4,F0..F3}` and the same per axis, `Prop/R{A,B}/{X,Y,Z}` + four bools each — the word table, driven by the receiver on remotes and by the encode layers on the wearer. `ObjectSync/Ch/Cycle` is word-channel's remote freshness counter. Everything else is `scratch:`.
- **Wire** (word-channel's synced params, all unsaved): 3 index bools + 2 8-bit int slots + 9 bool slots = **28 bits**, 6 batches, ~0.70 s full refresh at 60 fps. Plus `ObjectSync/Enable`, the entry's own synced bit, for **29 synced bits total** — the wire is what the transport costs, the total is what the avatar pays. The counter spans one loop, not two: `indexLoops` divides a pause-alias probability that `atomic: batch` does not have, so a second loop would cost an index bit and 18 `Sync` states for nothing.
- **Seam**: VRCFury `FullController` on the prefab root, `basis: mount-root` ↔ the FullController default `rootBindingsApplyToAvatar: 0` — every clip binding paths through the module's own rig, so the internal hierarchy names in **Rig** below are load-bearing. `globalParams` is exactly `ObjectSync/Enable`, which the module's own `Toggle` drives by name and so has to carry; every other param takes an instance prefix. That makes the *parameters* instance-safe and nothing else — the four collision tags are fixed strings VRCFury's prefixing does not reach, and two copies park their contact clusters at the same point, so a second instance needs a regeneration with its own tags, not a second drop of the prefab.
- **Dependencies**: **compose `anti-cull` alongside** — the decode runs only while a remote client evaluates the wearer's animator, and a view-culled wearer's prop freezes where it was last decoded. Distance-hide is not defeatable and is a declared limit, not a defect.
- **Required assets**: `assets/World.prefab` — the never-instantiated prefab `Rig` sources, which is what makes the frame the same world origin on every client. A bare origin node; do not instantiate or delete it.

## Rig

All three prefabs (root, `y/`, `y_double/`) are hand-maintained against this section; the numbers are `generate.py`'s CONFIG, and a retune happens there and lands here, never the other way round. Clip bindings name these paths verbatim, so a rename in the prefab silently unbinds the controller.

    Authority                              the measured transform; VRCParentConstraint → Anchor by
                                             default, re-pointed only for a different authority
    Rig/                                   VRCParentConstraint → World.prefab, the ONE component pinning both
                                             position and rotation; the park rides source0's ParentPositionOffset
                                             (73, 932, 233), which resolves in SOURCE space and so is world-fixed
      Prop/
        Coarse/
          Sender                           VRCPositionConstraint, 2 sources: Rig at weight 0.999664306640625,
                                             the prop's authority transform at 0.000335693359375 (= 2.75/8192)
          RecvX RecvY RecvZ                box receivers, face mode, size 6 on the read axis, localOnly
        Fine/
          Sender                           VRCPositionConstraint → the prop's authority transform; sphere sender, radius 0.05
          Anchor/                          VRCPositionConstraint → Rig; PositionOffset animated (cell centre)
            RecvX RecvY RecvZ              box receivers, face mode, size 6, localOnly
        Rot/
          Holder/                          VRCRotationConstraint → the authority transform; position pinned to Rig
            MarkA                          sphere sender at local (0, 0, 1)
            MarkB                          sphere sender at local (0, 1, 0)
          RecvAX … RecvBZ                  6 box receivers, face mode, size 2.5, localOnly
        Recon/
          ProxyA  ProxyB                   localPosition animated from the decoded components
          UpAim/                           VRCAimConstraint: AimAxis (0,1,0), source ProxyB, WorldUp None
            Recon                          VRCAimConstraint: AimAxis (0,0,1), source ProxyA,
                                             WorldUp ObjectRotationUp, WorldUpVector (0,1,0), WorldUpTransform UpAim
        Display/                           VRCPositionConstraint → Rig, PositionOffset animated;
                                             VRCRotationConstraint → Recon
    Anchor/                                the consumer's node; VRCParentConstraint shipped with an EMPTY
                                             source list (add your carry sources) + the Drop toggle
                                             (VRCFury), whose clip drives that constraint's FreezeToWorld
      Container/                           VRCParentConstraint, ONE source at fixed weight 1:
                                             source0 = Rig/Prop/Display; IsActive animated
                                             (1 on remotes with Enable on and Cycle ≥ 2, else 0);
                                             inactive it rides Anchor by hierarchy — your prop goes here

With more than one object, the surface trio gains the object name rather than staying bare — `Authority{Obj}`, and a suffixed `Anchor{Obj}/Container` pair per object, one anchor being one consumer-driven frame and one constraint following one reconstruction (COS ships the same suffixed-sibling shape as `SyncPositionA Target`/`SyncPositionA`) — each object gets its own collision-tag set (below), and the `m_IsActive` bindings pass from the enable's clips to the `Slice` layer's, which owns them alone.

A `y`-mode rig is this one with `MarkB`, `RecvAY`, the three `RecvB*`, `ProxyB` and `UpAim` deleted, and `Recon` re-parented to `Recon/` carrying `WorldUpType: Vector`, `WorldUpVector (0,1,0)` instead of the object-rotation up. Nothing else moves.

Ten rig facts are the design, not preferences:

| Fact | Value | Why it is that |
|---|---|---|
| Rig park | `(73, 932, 233)` m, derived from `rigSeed`, on the pin's **source** offset | Twelve receivers stacked where players spawn is how a contact cluster starts reading other people's dynamics; parking it is free, and the offset cancels because every client's copy uses the same one. It has to be the per-source offset: a constraint's own `PositionOffset` resolves in the constrained object's *parent* space, so putting the park there makes it ride the wearer's yaw and puts two clients' rigs hundreds of metres apart. |
| Receiver face | `size` 6 on the read axis (the per-shape maximum), 2.5 for rotation | `size` is the FULL extent. Face mode is a linear unlerp from the +Z face plane, so the read axis is the box's local Z and the other two only have to contain the sender. |
| Sender radius | 0.05 m, spheres | A sender is read by its nearest surface, so a sphere biases the reading by exactly its radius toward the face — a constant, folded into the walk's calibration. Changing the radius without regenerating skews every reading. |
| `Fine/Sender`, `Fine/Anchor`, `Fine/Recv*` under one `Rig` | not negotiable | This is what makes the fine stage bias-cancelling (below). Re-parenting the sender under the prop instead breaks it. |
| `Container` constraint | exactly one source, `Rig/<obj>/Display`, at a **fixed** Weight 1; `IsActive` is the animated property, and it is never 1 on the wearer's own client | `IsActive` because a weight cannot do this job. **Measured in play: a VRCParentConstraint holding one source at Weight 0 drives its transform to the captured rest every frame** — the same park `GlobalWeight 0` gives, not a no-op. Only two configurations are genuinely hands-off: `IsActive` false, and a source list of length **zero** (a leftover null second entry is not zero-length and re-arms the park). So the released prop rides `Anchor` only because the whole component is switched off; `../../docs/runtime.md` §Constraints carries the measurement. |
| Measured input stays upstream of driven output | `Authority` → `Anchor` by default, and never `Container` or anything under it | The entry drives `Container`; a measured default at or below it would have the rig measuring its own reconstruction, and the seam would close over the one node composition needs. `Anchor` is the frame that carries the consumer's intent — hand, freeze, multiplexer — so measuring it is measuring the prop's authority on the only client that measures at all (every receiver is `localOnly`, so a remote's measurement params are dead). |
| `Anchor`'s shipped constraint | `VRCParentConstraint`, **empty** source list, `Locked`, `IsActive` on; `FreezeToWorld` is animated by the composed Drop toggle and by nothing else | An empty source list writes nothing — the dependable hands-off no-op (`../../docs/runtime.md` §Constraints) — so the shipped prefab does nothing to `Anchor` until the consumer adds carry sources, and the pre-armed component is what lets the Drop clip bind a stable path. Two writers never meet on it: the consumer owns the source list, the Drop toggle owns `FreezeToWorld`, and the entry's document binds neither (`--check` holds that). |
| The gate's reach | the three subtree roots `Rig/<obj>/{Coarse,Fine,Rot}` carry the animated `m_IsActive` | Deactivating the subtree root kills a stage's senders and receivers together, and it is the *deactivation* that makes a clear stick — a live receiver re-asserts its parameter the next frame, which is exactly how a driver-only clear was measured failing. One writer owns this property: the enable's clips with one object, the `Slice` layer with several. |
| Readout margin off the face edges | ≥ `edgeGuard` (0.15 m) at both ends of every readout | It buys two things, and the second is easy to lose in a retune. It keeps the working volume off the ~20 mm saturation band and off the exact-zero floor; and because no legal in-volume reading can then be 0, a 0 is unambiguously "this receiver has not acquired", which is what every walk gates on before it samples. Shrinking the margin toward zero re-opens both. |
| Collision tags | `ObjectSync{Obj}{Coarse,Fine,RotA,RotB}` — the object name appears only when there is more than one, so a single-object build is `ObjectSyncCoarse` … `ObjectSyncRotB` | One tag per sender group keeps the four readouts off each other's senders, since all four clusters sit at the same point. **Per-object sets are not optional**: measured with two objects sharing one set, *neither* converges, because each receiver reads the strongest sender in range rather than its own. Receivers are `allowSelf` only and every contact is `localOnly`, so nobody else's rig is on the hook either. |

## How it works

**Position, per axis, in two stages.** A `VRCPositionConstraint` squeezes the prop toward the rig anchor — anchor weight `1 − g`, prop weight `g = coarseHalfSpan/range` — so the whole ±8192 m range lands inside ±2.75 m of a 6 m face receiver. An `IsLocal` successive-approximation walk (one state pair per bit, accepted above, rejected below) turns that reading into 13 bits naming a 2 m **cell**. The walk's accumulated cell index places a second anchor at that cell's centre, and a sender constrained to the prop is read at 1:1 by face receivers riding that anchor, which a second walk turns into 12 more bits. Anchor, receiver and sender share one hierarchy, so the float32 displacement the avatar's own distance from the world origin introduces appears identically on both sides of the subtraction and cancels bit-exactly — measured identical to the last bit with the avatar parked at 0 m and at 5 km.

**Why 13+12 and not 13+11.** The coarse stage does *not* cancel that displacement: at 8 km its world error runs to 1.16 m, more than half a cell, so the cell it picks can be one boundary out. The fine field is therefore **redundant** — it spans the cell plus twice the coarse error (4.4 m), not the cell (2 m) — so an off-by-one cell is still reconstructed exactly, at the cost of one bit per axis and no correction pass anywhere. That 4.4 m field sits inside the 6 m face with 0.8 m spare at each end, which is what keeps the readout off the two ways a face receiver lies: it saturates at 1.0 for about 20 mm past the face plane, and past that it reads exactly 0 — the same value as "nothing overlaps".

**Rotation without trig or physbones.** A rotation-only holder carries two markers on orthogonal 1 m arms; three face receivers per marker give six components, sent raw. Every client rebuilds the orientation with a `VRCAimConstraint` pair: `UpAim` aims +Y at proxy B, and `Recon` — **its child**, so the solver is forced to resolve `UpAim` first in the same frame — aims +Z at proxy A with `WorldUpType` **ObjectRotationUp** against `UpAim`. `ObjectUp` looks like the right token and silently degenerates to world up; it is never correct here. The `y` preset is a strict subset of the same rig — marker A's X and Z components on the wire, one `Recon` aim constraint with `WorldUpType` **Vector** `(0,1,0)`, no second marker and no `UpAim` — and it sends components rather than the heading itself because a parameter driver reads 0 from an AAP, so no walk can quantize a number a blend tree computed. Its two components are not two values, though: they are one heading. The two walks still run in **parallel** — serializing them would add a whole 12-bit ladder to every measure cycle of the configuration this preset exists for — but neither writes a word. Each ends by raising a staged-done flag, and a two-state **commit barrier** publishes all 24 bits in one driver frame once both flags are up, then lowers them, releasing the walks. It costs two frames, two scratch bools and one layer, and nothing in cycle time: the rotation path runs 17 frames against the position walk's 34, so the slice is unchanged. Full mode's six components genuinely are independent readings from two markers, so a torn set of them is a stale orientation rather than one no marker held, and they keep a walk and a commit each.

**The wire, and why nothing tears.** Each axis's coarse byte, fine byte and nine bools share one `group:`, so word-channel pins them into one batch and `atomic: batch` applies them together: an adjacent-cell coarse always lands with its matched fine, which reconstructs the same position, so cell-boundary flicker needs no hysteresis anywhere. Each rotation component's byte and four bools share a cross-kind group the same way, and `y` mode's two heading components share one group as well. The walks are multi-frame and that is safe — they run only on the wearer's own client, which cannot cull itself — but the handoff to the wire must not be, because word-channel's sender copies the word params on its own clock and would happily latch a half-written axis. So the walks fill scratch staging and one `Commit` state driver-copies a whole axis, all eleven words, in a single frame. The rotation walks are the same rule applied to whatever counts as one value: a commit each in full mode, and in `y` two parallel walks handing both heading limbs to one commit barrier.

**Two anchors, not one.** The measurement anchor rides the walk's running cell index — a staged value, mid-cycle, local. The display anchor rides the committed word and is side-agnostic: the wearer decodes its own prop from the same words a remote does. They cannot be one node, because sharing would mean committing the coarse word before the fine walk has run, which is exactly the torn axis the commit exists to prevent.

**Who drives the prop, and who never does.** `Container`'s constraint is the entry's whole reach into the consumer's hierarchy, and the `Follow` layer holds its `IsActive` at 1 exactly when `!IsLocal AND ObjectSync/Enable AND ObjectSync/Ch/Cycle ≥ 2` — a layer rather than a rung of the enable's blend tree, because `IsLocal` is a bool built-in and a tree reads 0 from it forever. The `Cycle` term is the late-joiner gate: a fresh clone's word table is all zeros, zero is cell 0, and cell 0 is the corner of the range, so engaging before a whole table has arrived would show the corner. Two tails rather than one because `atomic: batch` re-acquires at any counter value and can enter at a tail, taking its first increment one batch later (`../word-channel` §How it works owns that). Releasing does not test `Cycle`: the counter only climbs, so re-enabling engages on the frame the toggle returns. So the wearer's copy of the prop is never driven: it moves under whatever the consumer's own system gives it, with no decode in the path and no constraint to fight a grab, and only remotes reconstruct. Off is not a park anywhere — a deactivated constraint writes nothing, so the prop simply rides its parent on both sides. Switching the component rather than its weight is forced, not stylistic: the rig-facts table has the measurement.

**The enable, and what it does not gate.** `ObjectSync/Enable` is one synced bit, so wearer and remotes park together. Off, a 1D tree on it deactivates the three measure subtrees; each measuring layer takes an AnyState rung into a `Parked` state whose driver clears that layer's staging, residual, cell index and sense params — in that order, because a deactivated sensing component does not fall to zero, it *freezes* at its last live reading, and only a clear written after the deactivation sticks. What Enable does **not** gate is the wire: word-channel's 28 bits are allocated whether or not the prop is out, so gating it would save nothing and would cost the re-enable a full re-acquisition. The word table is left holding the last committed pose for the same reason — re-enabling shows the prop where it was, not at the origin.

**Coming back up is where a clear turns into a lie.** A cleared sense param reads 0, and 0 quantizes to cell 0 — the far corner of the range — so a walk that samples before its reactivated receivers have re-acquired publishes a confident wrong answer rather than nothing. Walks therefore gate on **receiver liveness**, not on a timer: a 0 reading is never legal in-volume, which is the edge guard's second job, and every walk-entry transition additionally requires the very params that walk is about to sample to read above a threshold derived from that same guard. Re-acquisition measured 7 frames in the emulator, is unbounded in principle, and the gate does not care. The road to any `Commit` likewise runs through every bit of the walk. Both are transition structure rather than a timing argument, and `--check` asserts both. The multi-object `Slice` layer is the same discipline at slice granularity — deactivate every other object's rig, clear, wait for this one's receivers to come live, then unblock its walks — with a bounded skip so a rig that never wakes yields its slice instead of starving the others, and a second AnyState rung making a walk that loses the rig mid-flight abandon rather than commit limbs measured against somebody else's senders.

**A gate is only safe where waiting can end.** A liveness wait survives exactly when its condition can be restored without the machine advancing past the wait, and one wait fails that test: the fine receiver rides the measurement anchor, and only a fresh coarse commit moves it. Teleport the prop out of its current cell while the walk is past the coarse stage and the fine sender leaves its box for good, so that wait blocks on a stage it can no longer reach — measured in a venue as 2 of 5 instantaneous teleports stalling the position stage indefinitely while rotation, whose markers cannot leave their boxes, kept tracking. The fine wait therefore carries the machine's one escape: after `fineEscapeFrames` of dead reading it abandons to `Idle`, which restages the cell, snaps the anchor and restores overlap. Every other wait is on a source that restores itself — the coarse receiver's box spans the whole working volume, and the rotation markers ride rigid arms — and `generate.py`'s liveness audit records which is which, with `--check` holding the document to it in both directions, so a new gate cannot be added without an entry saying why waiting there ends.

The failure mode that buys is the one this entry is built around: a rig that never wakes leaves its walk waiting and the wire holds the last committed pose. Nothing publishes a position it did not measure, and nothing waits on a reading that will never come.

**Empirical constants** (measured in the emulator; re-measure before changing any of them):

| Constant | Value | Where it lands |
|---|---|---|
| Coarse readout error, contact space | 0.39 mm worst case, avatar 4–8 km from world origin | `coarseNoise` in CONFIG; amplified by `range/coarseHalfSpan` to 1.16 m of world error, and the fine field is widened by twice that |
| Fine readout | float32-exact, zero noise; ≤1 frame stale under motion | why the fine stage sets system precision and the coarse stage's error costs bits, not accuracy |
| Fine-anchor settle | 1 frame to the cell centre, 2–4 frames to a coherent reading | `settleFrames` = 4 |
| Rotation error at 12 bits/component | 0.032° mean, 0.12° max, at 90° marker separation | `rotBits`; the bar it beats is Custom-Object-Sync's 0.176° |
| End-to-end position error, 2 m to 9.6 km | 0.7–1.8 mm, per axis inside `[−1.3, 0] mm` | the precision floor: the walk floors rather than rounds, so the residual is one-sided. Two terms make it up — one fine LSB (1.07 mm) of quantization, plus the float32 ulp at range (~0.98 mm at 8192 m) — and together they can exceed a single LSB, which is why the measured band runs past 1.07 mm. More bits would move neither term |

**Costs.** 374 states and 14 layers for the root build (11 and 18 layers for `y/` and `y_double/`); 34 frames (~0.57 s) per local measure cycle, plus however long the receivers take to come live after an unpark, ~0.70 s per wire refresh, ~1.3 s worst case end to end. A two-object build costs a slice each instead, ~0.8 s per slice. Twelve local-only receivers per object, zero physbones. The design also has a speed ceiling: sustained authority motion faster than roughly the fine field over one measure-plus-wire interval (≈ 7–8 m/s at 60 fps for the committed geometry) walks the fine sender out of its box every cycle, so the escape restages continuously and the display holds the last good commit rather than tracking — measured at 12 m/s as a clean hold, 1.5 m/s as normal ~1.3 s-lag tracking. A droppable prop never reaches it; a projectile consumer is out of scope by construction, not by tuning. `generate.py --check` holds regeneration byte-identical for all three committed documents, pins each against the one its `built/` was compiled from, and asserts packing, commit reachability, the liveness gate on every walk entry, the slice sequencing and the reconstruction shapes across all three — including that no driver anywhere reads or writes an AAP param, which is the shape of the defect that cost y-mode its first design.

## Verifying the install

Cheapest observable: **move `Anchor` (or whatever `Authority` is re-pointed at) with the wearer standing away from the world origin and facing away from +Z**. On the wearer, `Container` must track it frame-for-frame — the entry is not in that path at all (the ride is plain hierarchy), and any lag there means the local branch is driving something it must not. On a remote clone, `Container` reaches the same world pose inside about a second — millimetres of position, hundredths of a degree of rotation, the same value on both sides because both decode the same words. The off-origin, off-axis stance is the part that is easy to skip and the part that catches the whole class: a rig frame that has quietly stayed avatar-relative is indistinguishable from a correct one at the origin, so read `Rig` while you are there and require exactly `(73, 932, 233)` with identity rotation on both clients. Local and clone disagreeing by more than quantization is the decode reading a different word table, not the wire being slow; a clone's `Container` frozen while `ObjectSync/Ch/Cycle` climbs is the rig rather than the wire (`../word-channel`'s own §Verifying owns the `Cycle` reading). On a cold join the prop is *absent* rather than misplaced for the first two loops (~1.4 s) and then appears already at its synced pose — the `Follow` layer holds off until `Cycle ≥ 2`, so a late joiner never sees the range corner at all. For the drop path: with a hand source on `Anchor` and Enable on, Drop-on must freeze the wearer's prop on that frame and walk a clone to the same world pose inside the usual ~1.3 s; Drop-off must snap the wearer's prop back to the hand on that frame — any wearer-side lag on either edge means something other than `FreezeToWorld` is in the local path.

What the emulator structurally cannot show for this entry: everything `../word-channel` declares — its wire is this entry's wire — plus two that belong to the measure rig. **Contacts are never simulated against a remote clone**, so the twelve receivers are only ever exercised on the wearer's own client, and nothing here can reach a fault that needs another player's dynamics to appear — which is also why the park is an argument rather than a measurement: `(73, 932, 233)` is empty in a test scene by construction, where a real world may put geometry or another player's props through it. And **contact re-registration latency is the emulator's, not the client's**: a reactivated receiver takes 7 frames here, the shipping client is unmeasured, and the design gates on the reading rather than on that number precisely so the difference cannot matter.
